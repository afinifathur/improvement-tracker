# Copy this file to the project root as `.env` and fill in the secrets.
# The root `.env` is gitignored and read automatically by docker compose.

APP_NAME=ImprovementTracker
APP_ENV=production
APP_DEBUG=false
APP_KEY=
APP_URL=http://localhost:6004

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=improvement_tracker
DB_USERNAME=improvement
DB_PASSWORD=

MYSQL_ROOT_PASSWORD=

APP_PORT=6004
